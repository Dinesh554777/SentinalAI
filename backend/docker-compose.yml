version: "3.9"

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ENV_NAME=development
      - DEBUG=true
    volumes:
      - .:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
